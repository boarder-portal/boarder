.shuffleOptions {
  padding-left: 16px;
}

.shuffleAfterMovesCount {
  display: flex;
  align-items: baseline;
  margin-top: 8px;
}

.root {
  .MuiFormControlLabel-label {
    display: flex;
    align-items: baseline;
  }
}
