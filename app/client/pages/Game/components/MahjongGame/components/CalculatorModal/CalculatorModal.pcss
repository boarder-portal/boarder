.root {
  width: 1080px;
  min-height: 600px;
}

.tilesGrid {
  display: grid;
  grid-auto-rows: max-content;
}

.tile {
  &:not(.disabled) {
    cursor: pointer;
  }

  &.disabled {
    opacity: 0.5;
    pointer-events: none;
  }
}
