import { MAP_HEIGHT, MAP_WIDTH } from 'common/constants/games/bombers';

import { ISize } from 'common/types';

export const VIEW_SIZE: ISize = {
  width: MAP_WIDTH,
  height: MAP_HEIGHT,
};
export const CELL_SIZE = 20;
