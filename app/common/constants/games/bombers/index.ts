import { IGameOptions } from 'common/types/bombers';

export const DEFAULT_GAME_OPTIONS: IGameOptions = {
  minPlayersCount: 1,
  maxPlayersCount: 4,
};

export const MAP_WIDTH = 30;
export const MAP_HEIGHT = 20;
