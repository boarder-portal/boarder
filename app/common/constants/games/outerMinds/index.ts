import { GameOptions } from 'common/types/games/outerMinds';

export const DEFAULT_GAME_OPTIONS: GameOptions = {
  minPlayersCount: 2,
  maxPlayersCount: 4,
};

export const DRAFT_PHASE_CARDS_COUNT_TARGET = 6;
export const DRAFT_PHASE_DRAW_CARDS_COUNT = 3;
export const DRAFT_PHASE_PICK_CARDS_COUNT = 2;
